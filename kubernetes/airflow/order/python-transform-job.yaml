apiVersion: batch/v1
kind: Job
metadata:
  name: python-transform
spec:
  template:
    spec:
      containers:
      - name: python-transform
        image: samuelbeau/order-python-transform
        command: ["python", "main.py"]
        volumeMounts:
        - name: order-data-folder
          mountPath: /app/data/to_ingest
      restartPolicy: Never
      volumes:
      - name: order-data-folder
        hostPath:
          path: /home/ubuntu/kubernetes/airflow/order/data/to_ingest
          